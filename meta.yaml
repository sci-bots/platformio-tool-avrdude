package:
  name: platformio-tool-avrdude
  version: 1.60300.0

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - platformio

  run:
    - platformio-platform-atmelavr
